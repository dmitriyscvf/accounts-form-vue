# Accounts Management Form (Vue 3)

Тестовое задание на позицию **Vue Frontend Developer**.

## 📌 Описание
Реализована форма управления учетными записями.  
Приложение позволяет добавлять, редактировать и удалять учетные записи.  
Данные сохраняются в стейт-менеджере и восстанавливаются после перезагрузки страницы.

Макет использовался как ориентир. Основной упор сделан на логику, структуру и соответствие ТЗ.

---

## 🛠 Используемый стек
- Vue.js 3
- Composition API
- TypeScript
- Pinia
- Vite
- LocalStorage (для сохранения данных)

---

## ⚙️ Функциональность

### Учетная запись содержит:
- **Метка**
  - необязательное поле
  - максимум 50 символов
  - ввод нескольких значений через `;`
  - сохраняется в виде массива объектов `{ text: string }`

- **Тип записи**
  - `LDAP`
  - `Локальная`

- **Логин**
  - обязательное поле
  - максимум 100 символов

- **Пароль**
  - обязателен только для типа `Локальная`
  - максимум 100 символов
  - при выборе `LDAP` скрывается и сохраняется как `null`

- **Удаление учетной записи**

---

## 🧠 Логика работы формы
- Кнопка **“+”** добавляет новую пустую учетную запись в конец списка
- Учетная запись может быть полностью удалена
- Валидация выполняется:
  - для текстовых полей — при потере фокуса (`blur`)
  - для селекта — при изменении значения (`change`)
- При невалидных данных поля подсвечиваются
- При валидных данных запись сохраняется / обновляется в Pinia
- При обновлении страницы данные восстанавливаются из localStorage

---

## 📁 Структура проекта

